# 開発規約

## 設計と作業範囲

- 着手前に [README.md](README.md) と [docs/design.md](docs/design.md) の関連箇所を読む。
- 着手時に実コード・差分・最新CIを確認する。開発手順は [docs/development.md](docs/development.md)、検証は [docs/testing.md](docs/testing.md) を参照する。
- 仕様は設計書を基準とする。選択可能なモデルと、動作・精度の確認範囲を区別する。
- ユーザーの新しい指示で仕様が変わった場合は、設計書も更新して矛盾を残さない。
- 仕様の変更と通常の実装判断を区別する。実装判断や調査で解決できることは自律的に進める。
- ユーザーの判断が必要な質問は、推奨案を添えて一度に一つだけ行う。
- 細かい試作ごとの確認を要求せず、変更全体をまとめて試せるようにする。
- 文書の実装予定と、実際に利用できる機能・コマンドを明確に区別する。

## 開発環境

- React＋TypeScript＋Vite、Bun中心の構成とする。
- 起動・ビルド・テストはプロジェクトのLinux Dev Containerを標準とする。
- WindowsではWSL2を利用し、PowerShell固有の開発手順は追加しない。
- `.devcontainer` に必要なツールを定義し、Bunと互換性用Node.js 26系を用意する。
- ツールのバージョンと依存関係のロックファイルを固定し、CIも同じ環境定義を使う。
- パッケージの新規導入・更新では、公開から14日未満の版を除外する。直接・間接依存の両方を対象とする。
- Bunでは `bunfig.toml` の `minimumReleaseAge` を維持する。既存ロック済みの依存は再判定されないため、ロックファイルを取り込む場合も公開日を確認する。
- 例外は緊急セキュリティアップデートに限り、対象の版・脆弱性・例外理由を記録する。恒久的なパッケージ単位の除外や一律の制限解除はしない。
- Bun以外で導入する開発ツールにも同じ公開後14日の選定基準を適用する。
- Dev Container内ではコマンドを直接実行する。
- Codex本体はホストのstandalone版を共有する。プロジェクトで新規導入・更新・バージョン固定せず、ホスト側で管理する。
- 補助CLIの共有条件は開発ガイドに記載する。認証共有を変更する場合は影響を確認し、認証情報をファイルやログへ転記しない。
- 検証結果は実行したコマンドに基づいて報告する。過去の成功件数・個別環境の試行記録を恒久的な仕様文書へ転記しない。
- 実機の音声・WebGPU確認はホストのEdge／Chromeで行う。通常の確認用にGPU・音声機器のコンテナ転送を要求しない。

## 実装上の境界

- GitHub Pagesで配信できる静的アプリとし、専用バックエンドや利用者ログインを前提にしない。
- オフライン対応は対象外。モデルキャッシュは再取得を減らすために利用する。
- UI、練習の進行、音声処理、認識モデル、永続化を分離する。
- モデル固有の前処理・実行・後処理は交換可能な実装へ閉じ込める。
- 推論等の重い処理をUIスレッドから分離し、音声・モデルの大容量データを画面の状態管理に保持しない。
- 一時停止・戻る・設定変更時に音声やタイマーを適切に停止し、遅延した結果を別のお題へ適用しない。
- 録音・認識待ち・モデルの資源を解放し、設計書の件数・容量上限を守る。
- 保存形式にはバージョンを持たせ、既存データの互換性と復元失敗時の保全を考慮する。
- 音声認識の不一致を発音の誤りと断定しない。認識結果と練習への反応を区別する。
- UIは日本語で、聴覚だけに頼らず状態が分かる表示とキーボード操作を備える。

## 検証

- 変更に応じた型チェック・ビルド・テストを実行し、実行結果と未確認事項を報告する。
- 自動テストは練習の状態遷移、保存・削除・復元、主要な画面操作を重点とする。
- 特に一時停止・繰り返し・戻る・区切り到達・遅延認識結果・処理待ち上限・容量上限を確認する。
- 通常CIでは録音や認識結果を模擬し、毎回大きなモデルを取得しない。
- モックの成功を、実モデルの認識精度・WebGPU互換性・補聴器の聞こえ方の検証と混同しない。
- 実モデルは日本語の1音・無意味2音・単語を区別して確認する。
- 文書だけの変更では、不要なアプリテストを追加せず、リンク・記述・差分を確認する。

## 改行とファイル

- `.gitattributes` でテキストはLF、`.bat`／`.cmd` はCRLFと明示する。
- 音声・画像・モデル等のバイナリに改行変換を適用しない。SVG等のテキスト形式は区別する。
- `.editorconfig` でUTF-8・LF等の編集規約をそろえる。
- 他の変更に無関係な改行変換や全体整形を混ぜない。
- ダウンロードしたモデル、録音、ビルド成果物、個人用バックアップをGitへ追加しない。

## 個人情報・個人環境の扱い

- 認証情報、秘密鍵、トークン、および会話・調査で知った非公開の個人情報（氏名・連絡先・健康情報等）をコミットしない。
- 個人のPC・GPU・音声機器の機種、ネットワーク構成・接続方法、ホスト名・IPアドレス、ユーザー名・ローカルパス、個人環境での検証記録をコミットしない。会話での共有や調査への使用許可を、リポジトリへの掲載許可と扱わない。
- この規約はコードだけでなく、文書・コメント・設定・テストデータ・画像・ログ・生成物にも適用する。コミットメッセージ、PR・Issueのタイトルや本文にも転記しない。
- 共有の開発環境・必要動作条件は、個人と結び付かない形で記載する。設定例は環境変数や架空の値を使い、個人の実値を流用しない。プロジェクトで定義するコンテナ内の共通パスや、一般的な手順は記載してよい。
- コミット前に追加・変更内容を確認し、生成物や添付資料への混入も調べる。既存の混入を見つけた場合は値を再掲せず報告し、履歴の書き換えや外部での削除は別途許可された範囲で行う。

## データとGit

- 音声認識は端末内モデルと、利用者が選択するブラウザ標準（Web Speech API）を提供する。ブラウザ標準では録音音声が認識サービスへ送信される場合があることを表示する。無断で認識方式を切り替えない。オンライン見本音声へ渡すお題テキストと録音データを混同しない。
- 診断情報から録音・練習内容を除き、自動送信しない。
- 教材・音声・モデル・外部コードを導入する際は、利用条件と必要な出典表示を確認する。
- Git操作前に対象リポジトリ・ブランチ・差分を確認する。他の作業の変更を無断で取り消したり混ぜたりしない。
- コミットメッセージは日本語で、変更内容を具体的に記載する。
- コミット・push・公開は、会話中のユーザーの許可範囲に従う。許可済みの操作を繰り返し確認しない。
